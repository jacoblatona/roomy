import { atom } from "jotai";

export const isLoginModalOpenAtom = atom(false);
export const isRoomModalOpenAtom = atom(false);
export const isAskQuestionModalOpenAtom = atom(false);
export const isAnswerQuestionModalOpenAtom = atom(false);
